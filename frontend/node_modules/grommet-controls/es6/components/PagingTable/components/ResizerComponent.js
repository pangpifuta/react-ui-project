import React from 'react';

import { StyledResizerComponent } from '../StyledPagingTable';

var ResizerComponent = function ResizerComponent(props) {
  return React.createElement(StyledResizerComponent, props);
};

export default ResizerComponent;