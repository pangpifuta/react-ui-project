import React from 'react';
import { DateInput } from '../DateInput/index';
import { withFormField } from '../withFormField';

export var DateInputField = withFormField(function (props) {
  return React.createElement(DateInput, props);
});