'use strict';

exports.__esModule = true;

var _reactDesc = require('react-desc');

exports.default = function (Element, Name, Description) {
  var DocumentedElement = (0, _reactDesc.describe)(Element).description(Description).usage('$ npm install grommet-controls\n\n    import { Form, ' + Name + ' } from \'grommet-controls\';\n\n    <Form>\n      <' + Name + ' name=\'xxx\' label=\'xxx\' validation={[validators.required()]} />\n    </Form>  \n');

  DocumentedElement.propTypes = {
    name: _reactDesc.PropTypes.string.description('Name of the field.').defaultValue(false),
    label: _reactDesc.PropTypes.string.description('String label to be used for the field, will also be used in validation errors.').defaultValue(false),
    controlLabel: _reactDesc.PropTypes.node.description('Can be used if the underlying control has a label property (ie CheckBox).').defaultValue(false),
    inField: _reactDesc.PropTypes.bool.description('Set to false if the field should not be placed inside a FormField.').defaultValue(true),
    validation: _reactDesc.PropTypes.array.description('Validation rules, will be chained.').defaultValue(false)
  };

  return DocumentedElement;
};