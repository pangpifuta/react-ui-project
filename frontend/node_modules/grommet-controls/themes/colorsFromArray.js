"use strict";

exports.__esModule = true;

/* eslint-disable no-param-reassign */

exports.default = function (colors, array, prefix) {
  return array.forEach(function (color, index) {
    colors[prefix + "-" + (index + 1)] = color;
  });
};